<template>
    <div>
        <q-dialog v-model="$store.modals.list.{{ DIALOG_NAME }}">
            <q-card style="width: {{ POPUP_WIDTH }}; max-width: 80vw;">
                <DialogTitle :modelId="modelId" :title="$t('{{ DIALOG_TITLE }}')" />
                {{ SHOW_CONTENT }}
            </q-card>
        </q-dialog>
    </div>
</template>

{{ SCRIPT }}