
                <!-- ================= {{ NAME }} ================= -->
                <div class="{{ CLASS_FIELD }} pb-5"{{ SHOW_CONDITION }}>
                    <CheckboxInput
                        :form="form"
                        name="{{ NAME }}"
                        :label="$t('{{ LABEL }}')"
                    />
                </div>
                <!-- ================= ./{{ NAME }} ================= -->