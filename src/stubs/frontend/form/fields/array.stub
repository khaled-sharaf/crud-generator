
                <!-- ================= {{ NAME }} ================= -->
                <div class="{{ CLASS_FIELD }}"{{ SHOW_CONDITION }}>
                    <ArrayOfText
                        :form="form"
                        type="{{ TYPE }}"
                        name="{{ NAME }}"
                        :dense="form.{{ NAME }}.length > 1"
                        :label="$t('{{ LABEL }}')"
                        item-label{{ REQUIRED }}{{ TRANSLATABLE }}
                    />
                </div>
                <!-- ================= ./{{ NAME }} ================= -->