
                <!-- ================= {{ NAME }} ================= -->
                <div class="col-12 col-padding pb-5">
                    <EditorInput
                        :placeholder="$t('{{ LABEL }}')"
                        :form="form"
                        name="{{ NAME }}"
                        :label="$t('{{ LABEL }}')"{{ REQUIRED }}{{ TRANSLATABLE }}
                    />
                </div>
                <!-- ================= ./{{ NAME }} ================= -->