
                <!-- ================= {{ NAME }} ================= -->
                <div class="{{ CLASS_FIELD }}">
                    <PasswordInput
                        :form="form"
                        name="{{ NAME }}"
                        :label="$t('{{ LABEL }}')"{{ REQUIRED }}
                    />
                </div>
                <!-- ================= ./{{ NAME }} ================= -->