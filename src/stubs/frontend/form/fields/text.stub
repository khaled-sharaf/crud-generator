
                <!-- ================= {{ NAME }} ================= -->
                <div class="{{ CLASS_FIELD }}">
                    <TextInput
                        :form="form"
                        type="{{ TYPE }}"
                        name="{{ NAME }}"
                        :label="$t('{{ LABEL }}')"{{ REQUIRED }}{{ TRANSLATABLE }}
                    />
                </div>
                <!-- ================= ./{{ NAME }} ================= -->